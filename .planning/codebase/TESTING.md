# TESTING

## Current State
*   **Framework:** No formalized Python testing framework (e.g., `pytest`) is completely integrated yet.
*   **Coverage:** 0% automated coverage.
*   **Methodology:** Currently relies on manual verification checks (mocking / test files) and browser inspections.

## Targets
*   We need `pytest` for backend units to test the `verifier.py` logic and `database.py` CRUD operations.
*   We need Vitest/Jest for React components.
